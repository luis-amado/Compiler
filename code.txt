program main {
    var n, r, a : int;
    var fk: bool;
    var s : string;
    var u : char;

    procedure test {
        begin;
        end;
    }

    begin;
        my_func();
        a := not(b and c);
        a++;
    end;
}